# Automate Angular Build Process 🚀

This repository demonstrates how to automate the build process of an Angular
application using **GitHub Actions (CI/CD)**.

The Angular project is available on the `second` branch and is used to trigger
automated workflows on every push.

---

# MyAngularAp

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 21.1.0.

## Development server

To start a local development server, run:

```bash
ng serve
